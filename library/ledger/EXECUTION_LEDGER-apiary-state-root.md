# Execution Ledger: Apiary State-Root Migration (fleet ADR-0003)

Single source of truth for sequencing the fleet-wide migration from the legacy `~/.honeycomb` shared home to the neutral `~/.apiary/` fleet root with per-product subdirectories. Status legend: OPEN / IN PROGRESS / DONE / VERIFIED / BLOCKED.

**Authoritative decision record:** superproject [`library/knowledge/private/architecture/ADR-0003-fleet-directory-ownership-and-neutral-state-root.md`](../knowledge/private/architecture/ADR-0003-fleet-directory-ownership-and-neutral-state-root.md) (Status: Accepted). Mirrors: nectar ADR-0005, doctor ADR-0003, hive ADR-0005, honeycomb adr/0008. **Edit the authoritative copy, then re-sync all four mirrors; never edit a mirror in isolation.**

**Fleet decisions (locked, 2026-07-04):**
1. Fleet root name: `~/.apiary/` (confirmed over `~/.doctor/`); per-product subdirs `doctor/`, `honeycomb/`, `nectar/`, `hive/`; shared surface (`registry.json`, `device.json`, `install-id`) at the root, doctor-managed. `~/.deeplake/` unchanged.
2. XDG rule: honor `$XDG_STATE_HOME/apiary` on Linux only when `$XDG_STATE_HOME` is explicitly set; the default is `<homedir>/.apiary` on every OS; no `~/.local/state` default. The canonical `resolveFleetRoot` chain lives in ADR-0003 "Resolved decisions"; every repo implements it verbatim (mirrored, never imported).
3. Registry compatibility window contract: doctor creates the root and migrates the registry wholesale on its own first boot; every writer targets `~/.apiary/registry.json` when the fleet root directory exists, else the legacy `~/.honeycomb/doctor.daemons.json`, never both; doctor reads new-first and merges (new wins per daemon `name`, legacy-only entries merge additively, no write-back to legacy); the fallback is removed only when every supported install path ships its migration.

**The planning-gap corrective (2026-07-04):** the four per-repo PRDs were originally authored in parallel against an ADR with two unresolved ambiguities (the XDG reading and the registry handshake), and each invented its own default. Both are now resolved in ADR-0003 "Resolved decisions"; all four PRDs were reconciled to CITE the ADR rather than restate it. Any future contract change goes into the ADR first, then re-syncs outward.

---

## The four per-repo PRDs

| Repo | PRD | Location | Scope | Status |
|---|---|---|---|---|
| doctor | PRD-004 `apiary-fleet-root-migration` (004a-c) | `doctor/library/requirements/backlog/prd-004-apiary-fleet-root-migration/` | Shared root helper; relocation of the fleet-shared surface (registry, device.json, install-id); registry merge rule; supervision continuity incl. telemetry trust-root extension | OPEN (backlog) |
| honeycomb | PRD-072 `apiary-state-root-migration` (072a-d) | `honeycomb/library/requirements/backlog/prd-072-apiary-state-root-migration/` | Largest surface (original `~/.honeycomb` owner): pid/lock cutover, seven state families, fleet-shared writes, service-unit root pinning | OPEN (backlog) |
| nectar | PRD-020 `apiary-state-root-migration` (020a-c) | `nectar/library/requirements/backlog/prd-020-apiary-state-root-migration/` | Root helper + path adoption; one-time migration + legacy fallback; service unit + doctor-registry adoption. PRD-019 brooding state reconciled to `~/.apiary/nectar/projects.json` | OPEN (backlog) |
| hive | PRD-010 `apiary-state-root-migration` (010a-c) | `hive/library/requirements/backlog/prd-010-apiary-state-root-migration/` | Root helper + path constants; first-boot migration + pid/lock continuity; registry coordination + mid-migration portal honesty | OPEN (backlog) |

## Sequencing (build order rationale)

| Wave | Work | Depends on | Status |
|---|---|---|---|
| W1 | doctor PRD-004: root helper, fleet-root + shared-surface creation/migration, registry merge rule, trust-root extension | ADR-0003 (Accepted) | OPEN |
| W2 | honeycomb PRD-072, nectar PRD-020, hive PRD-010, in any order or in parallel: each product's own subdir migration + write-side registry contract adoption | W1 shipped (doctor tolerates both locations from W1 on, so W2 products can land independently) | OPEN |
| W3 | Superproject installer (`install.sh` / `install.ps1`, ADR-0002 surface): mint `~/.apiary/install-id`, write registry entries per the window contract, pin the resolved root as `APIARY_HOME` into service environments, Windows LocalSystem install-time home capture | W1 (the paths exist) | OPEN |
| W4 | Window close: remove every legacy `~/.honeycomb` fallback (read and write side) across all four repos | Every supported install path ships its migration (W1-W3 VERIFIED) | OPEN |

**W4 is the close-out gate.** Each repo's PRD requires legacy-window code to carry a removal-criterion comment (e.g. hive rr-AC-7) so W4 is a deletion sweep, not an archaeology project.

## Cross-repo invariants to verify at each wave

| ID | Invariant | Verify against |
|---|---|---|
| X-1 | All four repos implement the identical `resolveFleetRoot` chain (ADR-0003 "Resolved decisions"), anchored on `os.homedir()`, never `process.cwd()` | doctor 004a, honeycomb 072a, nectar 020a, hive 010a |
| X-2 | No product writes into another product's subdir; shared files only at the root, written per ownership (doctor manages; others write only their own registry entry) | all four PRDs |
| X-3 | Registry writers follow the window contract (new when the fleet root exists, else legacy, never both); only doctor merges | doctor 004b, honeycomb 072c, nectar 020c, hive 010c |
| X-4 | Advertised registry paths (`pidPath`, `telemetryDbPath`) match the paths the product actually writes, at every moment in the window | honeycomb 072c, nectar 020c, hive 010b/c |
| X-5 | Migration is one-time, idempotent, additive; no legacy file deleted unless successfully migrated; readers fall back while the window is open | all four PRDs |

## Remaining repo-local open items (not fleet-blocking)

| Repo | Item | Where |
|---|---|---|
| doctor | Telemetry trust-root binding tightness (per-own-name recommended); pid-path existence-check timing (boot/reload recommended); legacy marker name | 004b/004c open questions |
| honeycomb | Registry path-string convention (`~`-literal vs resolved absolute) - shared question with hive, settle once with doctor 004b; dual pid stamping during the window; memory-mount path shape dual recognition; install-id consolidation posture | 072 index open questions |
| nectar | pid/lock migrate-or-fresh (fresh recommended); daemon-writes-registry-on-upgrade refresh; install-id read order vs installer timeline; launchd `logs/` subdir name | 020 index open questions |
| hive | Root-helper distribution (mirror as `src/shared/` module recommended); `--home=` only on `hive install`; legacy launchd log tail copy (no recommended); registry `pidPath` convention (same question as honeycomb's) | 010 index open questions |

**One item to settle before W1 implementation starts:** the registry `pidPath`/`telemetryDbPath` string convention (tilde-literal with doctor expanding under the SAME resolved root, versus resolved absolute paths). Honeycomb 072c and hive 010c both flag it; it belongs to doctor 004b as the registry contract owner. Settle it there, then note the answer here.

## Smoker run 2026-07-04 (implementation)

**Scope:** nectar PRD-019 (019a-d), nectar PRD-020 (020a-c), doctor PRD-004 (004a-c), honeycomb PRD-072 (072a-d), hive PRD-010 (010a-c). 100% of acceptance criteria tracked below.

**Branches:** doctor `feature/prd-004-apiary-fleet-root`; honeycomb `feature/prd-072-apiary-state-root`; nectar `feature/apiary-root-and-activation`; hive `feature/apiary-root-and-nectar-activation`; superproject `feature/smoker-apiary-state-root`.

**Contract resolution during recon (2026-07-04):** the registry path-string convention is settled as ADR-0003 Resolved decision 4: writers put RESOLVED ABSOLUTE `pidPath`/`telemetryDbPath` into registry entries (never tilde-literals); doctor expands legacy tilde-literal entries during the window and validates telemetry paths against its resolved per-product trust roots plus the legacy root. Propagated to all four ADR mirrors.

### Wave plan

| Wave | Worker | Repo / scope | Model (routing per `.cursor/model-comparison-matrix.md`) | Exit criteria |
|---|---|---|---|---|
| W1-D | doctor implementation Bee | doctor PRD-004a/b/c | `claude-opus-4-8-thinking-high-fast`: security-relevant trust-root logic + the fleet-critical registry merge; deep-reasoning multi-file refactor routes to Opus | All 004 ACs DONE; doctor typecheck+tests green |
| W1-H | honeycomb implementation Bee (`typescript-node-worker-bee` profile) | honeycomb PRD-072a/b/c/d | `claude-opus-4-8-thinking-high-fast`: the largest surface, tiered import architecture, strict `npm run ci` gate; correctness dominant | All 072 ACs DONE; `npm run ci` green |
| W1-N | nectar implementation Bee | nectar PRD-020a/b/c | `gpt-5.3-codex-high-fast`: config/service-unit/registry plumbing + migration mechanics; CLI/DevOps automation routes to Codex | All 020 ACs DONE; nectar build+typecheck+tests green |
| W1-V | hive implementation Bee | hive PRD-010a/b/c | `composer-2.5-fast`: pattern-following constant rewiring + migration in an existing idiom; IDE-bound agentic coding routes to Composer | All 010 ACs DONE; hive typecheck+tests green |
| W2-N (after W1-N) | nectar activation Bee | nectar PRD-019a/b/d | `claude-opus-4-8-thinking-high-fast`: multi-root daemon supervisor is the deepest architectural change of the run | All 019a/b/d ACs DONE; nectar gate green |
| W2-V (after W1-V and W2-N API contract) | hive dashboard Bee | nectar PRD-019c (hive repo) | `composer-2.5-fast`: React dashboard + wire additions following existing FolderPicker/wire idioms | All 019c ACs DONE; hive gate green |
| W3 | `security-worker-bee` (armed `security-stinger`), then `quality-worker-bee` (armed `quality-stinger`) | all four repos | as dispatched | Zero Critical/High security findings; QA clean at medium+ |

**W3b quality results:**
- doctor: CLOSED CLEAN (2026-07-04). QA passed 32/32 ACs with 3 Warnings; ALL remediated (legacy install-lock staleness check with 5 named tests; workspace migration skips + logs under a pinned DOCTOR_WORKSPACE_DIR; c-AC-8 Windows drive-letter/drive-relative/UNC containment tests added and executed on win32, existing code passed all four unchanged) plus all 3 Suggestions (doc comments, dead exports removed, malformed-registry banner names the actually-broken file via RegistryError.registryPath). Orchestrator independently re-verified: typecheck OK, 60 files / 667 tests green (up from 655), home canary clean. QA report: doctor/.../prd-004-.../qa/2026-07-04-qa-report.md. SHIP-READY pending sibling fix cycles.

- hive: CLOSED CLEAN (2026-07-04). QA passed PRD-010 with 5 Warnings + 019c clean; ALL five Warnings and the Suggestions remediated (APIARY_HOME pinned into launchd/systemd units when the root is non-default, with the Scheduled-Task env limitation documented in code + PRD-010a; best-effort legacy cleanup; ENOENT-only fallbacks for registry + token reads; rc-AC-1 preservation and rc-AC-2 ordering tests; rr-AC-5/6 named tests; 019c controls disabled pre-hydration). Orchestrator independently re-verified: typecheck OK, 56 files / 396 tests green (up from 384), home canary clean. Reports: hive/.../prd-010-.../qa/ and nectar/.../prd-019-.../qa/ (019c-named). SHIP-READY pending sibling fix cycles.

- nectar: CLOSED CLEAN (2026-07-04). QA passed both PRDs with 8 Warnings total; ALL code-actionable items remediated: malformed-registry boot is fail-soft (loud log, no clobber, install verb stays fail-loud, plus a full-daemon boot-survival test); advertised telemetryDbPath gated on post-move existence (heals on retry boot); b-AC-4 test added; collapseGlobStars semantics + bounded-time regression tests (pathological input ~1ms vs 250ms ceiling); b-AC-6 persist error redacted with raw failure routed to the daemon log; the PRD-011b projection pre-warm rewired PER PROJECT into context start (PREFERRED option), dead seams deleted, and a latent stale-enricher-hydrate bug this exposed also fixed; c-AC-6 deferral annotated; in-repo security record written. Orchestrator independently re-verified: build + typecheck OK, non-live suite 716 tests / 713 pass / 0 fail, home canary clean. SHIP-READY pending honeycomb.

- honeycomb: CLOSED, RE-AUDIT VERDICT PASS WITH WARNINGS (2026-07-04). Initial audit BLOCKED with 2 Criticals (skillify lock legacy hardcode; telemetry SQLite mover-failure defeat) that the green `npm run ci` could not see (production defaults untested). Fix cycle closed both Criticals + all 4 Warnings + Suggestions; the re-auditor re-attacked its own sequences and confirmed closure (skillify default provably never touches `~/.honeycomb`; mover-failure opens the legacy DB, never mints-and-strands; window-only dual-stamp; CLI migration trigger with zero-writes fast path). Gates re-verified twice (worker + orchestrator + re-auditor): `npm run ci` exit 0, 386 files / 4127 tests / SQL audit OK. ONE Warning carried forward by deliberate deferral (below the block bar, tracked for the W4 window close): run the TRUE packaged-upgrade smoke once a pre-072 installable build exists; the in-repo upgrade-sequence integration test stands in until then. New Suggestion noted in PRD-072c: in the mover-failure fallback corner the registry briefly advertises the new telemetryDbPath while writes land at the legacy DB (self-heals on the next successful move; confirm doctor tolerates an advertised path with no file). QA report updated in place with the re-audit section. SHIP-READY.

**W3a security results:**
- doctor: CLEAN (2026-07-04). No Critical/High; zero code changes. Trust-root coercion held against traversal / symlink-string / drive re-anchoring / UNC / case / tilde / prefix-confusion / malicious entry-name attacks (entry name is filename-safe-validated before building the per-product root; validated path == opened path, no TOCTOU). 3 Informational/Low documented, not fixed (non-realpath symlink containment is pre-existing and documented in safe-path.ts; default-mode device.json carries no secret; registry copy is non-atomic but fail-safe). Gates: typecheck clean, 654/654, npm audit 0. Ready for quality.
- hive: CLEAN after remediation (2026-07-04). No Critical/High; 3 Medium/Low FIXED in place: F-1 lock-dir mkdir mode 0o700 parity, F-2 REJECT a relative `$XDG_STATE_HOME` (would have cwd-anchored the fleet root), F-3 registry mkdirp mode 0o700. CSRF posture of the new brooding POST documented as pre-existing and surface-wide (inherited local-mode posture, not introduced by this branch). Gates: typecheck clean, 384/384, npm audit clean. Report: hive/library/qa/security/2026-07-04-security-audit.md. Ready for quality.

- honeycomb: CLEAN after remediation (2026-07-04). No Critical/High; 2 Medium FIXED in place: (1) `resolveFleetRoot` honored RELATIVE `APIARY_HOME`/`XDG_STATE_HOME` verbatim, cwd-anchoring ALL state incl. the machine key (now: env roots honored only when absolute; installer `--home=` resolves relative flags against installer cwd at capture time); (2) the pre-tool-use memory-mount gate missed Windows backslash and case-varied shapes, letting reads/writes bypass to the real filesystem (now normalized + case-insensitive, classifyPath untouched). 2 Low documented (systemd unit escaping symmetry; legacy dual-stamp recreates `~/.honeycomb` each boot, tied to window close-out). Gates: `npm run ci` exit 0 (380 files / 4096 passed / 12 skipped, SQL audit OK), npm audit 0. Report: honeycomb/library/qa/security/2026-07-04-security-audit-prd-072-apiary-state-root.md. Ready for quality.

- nectar: CLEAN after remediation (2026-07-04). 1 High FIXED: ReDoS in the git-absent gitignore parser (consecutive `**/` runs compiled to overlapping unbounded quantifiers; exponential blowup empirically confirmed; fixed via `collapseGlobStars`, semantics preserved, sub-ms after). 1 Low FIXED (0o700 on the brooding-state dir). 3 Lows documented (loopback verbose errors, lexical root guard, temp names). Checked clean: SQL guards, loopback binding, POST validation + 1 MiB cap, token redaction, containment. Gates: build/typecheck OK, non-live suite 702/702. Ready for quality.

**Cross-repo consistency: RESOLVED AND ALIGNED (2026-07-04, orchestrator).** The relative-env-root finding (surfaced independently by the hive and honeycomb audits) is closed fleet-wide with ONE rule: env roots (`APIARY_HOME`, `XDG_STATE_HOME`) are honored ONLY when absolute, checked with `path.win32.isAbsolute` (strict superset, correct on every host); relative values fall through the chain; the installer resolves a relative `--home=` against its own cwd at capture time. Applied: honeycomb (by its audit), nectar + doctor (orchestrator, matching implementation + regression tests), hive (orchestrator, replacing the divergent resolve-against-home semantics of rr-AC-3; test updated). Pinned into ADR-0003's canonical chain as a security amendment and re-synced to all four mirrors. Re-verified after alignment: nectar 703/703 non-live, doctor 655/655, hive 384/384, typechecks clean, no home leak. (honeycomb unchanged by the alignment; its gate was green post-audit at 4096 passed.)
| W4 | ship | per repo | main thread | Commits, pushes, PRs open, CI monitored |

Routing note (honest): the beekeeper roster is Hivemind-tuned and names no implementation Bee for the nectar/doctor/hive codebases; per the roster's own rule, no Bee is improvised. Implementation workers are dispatched as general-purpose sub-agents armed with each repo's `AGENTS.md` and PRD set as their operating manual; honeycomb's worker follows the `typescript-node-worker-bee` profile since that stack matches. The W3 close-out uses the real `security-worker-bee` / `quality-worker-bee` agents per the arming contract.

**Tenancy FINAL (2026-07-04 16:15, user-directed):** the user does NOT want the local login on OSPRY. `~/.deeplake/credentials.json` now permanently carries the `github_ci` token, org `4ad849af...`, workspace `apiary_ci`; the OSPRY login is preserved at `~/.deeplake/credentials.json.ospry-backup` if ever needed. The earlier restore-at-close-out step is superseded and no restore is owed. (History: swapped to apiary_ci 13:42, briefly restored to OSPRY 16:08 per the original plan, re-swapped to apiary_ci 16:15 on user direction.)
**Tenancy correction (2026-07-04 13:42, user-directed):** local agent live tests were resolving `~/.deeplake/credentials.json` = the user's personal OSPRY org (`71f2566d-a545-4831-9339-3722dfd73471`, workspace `default`), NOT the CI tenancy. Corrected: the file now carries the `github_ci` token, org `4ad849af-c8b5-437f-9271-d62a52afeda2`, workspace `apiary_ci` (funded), with the original preserved at `~/.deeplake/credentials.json.ospry-backup`. RESTORE THE BACKUP AT CLOSE-OUT (W4). Caveat: any nectar live-test rows written before 13:42 may have landed in OSPRY/`default`; the W1-N verification pass must re-run nectar live tests under `apiary_ci`, and OSPRY/`default` should be checked for stray `hive_graph` test rows during W3.

**W4 SHIPPED (2026-07-04 16:1x):** all five branches committed, pushed, PRs open: doctor#11, honeycomb#229, nectar#21, hive#12, the-apiary#9 (ledger + ADR resolutions + submodule pointers; merge the submodule PRs first). CI watcher dispatched across all five. OSPRY credentials restored from backup at 16:08. Remaining tracked items for the WINDOW CLOSE (not this ship): the true honeycomb packaged-upgrade smoke once a pre-072 build is installable; removal of every legacy `~/.honeycomb` fallback when all supported install paths ship the migration; the nectar per-project enricher fast-follow (optional); raising the fresh-workspace ingest-visibility lag with Activeloop.

**W4 CI round 1 (2026-07-04 16:53):** doctor#11 GREEN; the-apiary#9 no CI checks, CodeRabbit review completed. Three PRs RED with deterministic CROSS-PLATFORM failures (the whole run built on Windows; CI runs macOS/ubuntu), none matching the Deeplake-lag pattern: honeycomb#229 fleet-store migration fallback test ENOTDIR + an Aikido scan check; nectar#21 isPathologicalRoot POSIX guard test; hive#12 Windows XML staging test asserted on POSIX. Three parallel fix cycles dispatched (workers authorized to commit+push to their PR branches); CI re-watch follows.
- hive#12: GREEN on all three platforms (2026-07-04 16:59, run 28719410902). Root cause: a hardcoded backslash fixture only matched `path.join` output on Windows; fixed by deriving the expected path from `resolveStagedWindowsTaskPath` itself (commit 5f10da1). Remaining `C:\` fixtures audited as safe.
- honeycomb#229: quality gates GREEN on both Node lines (2026-07-04 17:16, commit 302e753); Windows smoke, CodeQL, Secret gate, CLA, CodeRabbit all pass. Root cause was a genuine cross-platform BUG in the move primitive, not just a test: `rmSync(force:true)` suppresses only ENOENT, and on POSIX the cleanup under a file-blocked destination threw ENOTDIR out of a never-throw contract (proved with a WSL repro). Fixed via a guarded `bestEffortRm` + defense-in-depth in `resolveTelemetryDbPathForOpen`; also renamed a secret-looking test fixture. ONE CHECK REMAINS, PARKED BLOCKED: the Aikido Security check. Disposition: scanner re-keying noise (findings "solved+introduced" at shifted lines on files carrying 20+ pre-existing MAIN findings of the same readFileSync/join rule; CodeQL + Secret gate + the branch's dedicated security audit are all clean; the one real signal, the sk-live-like fixture, was remediated). EXACT ASK: a human with Aikido workspace dashboard access must triage/ignore the re-keyed findings on PR #229; the API/MCP paths 400 and the check exposes no annotations, so this is not automatable from here.
- nectar#21: GREEN on all three platforms (2026-07-04 17:03, run 28719514162, commit 3edba93). Root cause: `isPathologicalRoot` parsed paths with the HOST `node:path`, so win32 fixtures failed on POSIX runners. Fix makes the guard a pure function of (path, platform) via `path.win32`/`path.posix`, keeps POSIX $HOME case-sensitive, makes the System32 leg Windows-only (no POSIX false positive), AND closes a real production gap the failure surfaced: the $HOME guard now defaults to `os.homedir()` (the daemon passed no explicit home, so the shipped guard never actually protected the real home). 717 non-live tests, 714 pass.

### AC ledger

Status is per-AC. Sub-PRD AC text lives in the source PRD; IDs here are authoritative pointers. Owner = wave ID. All OPEN at run start.

| PRD | ACs (all start OPEN) | Owner | Status |
|---|---|---|---|
| doctor 004 index | AC-1..AC-9 | W1-D | DONE (2026-07-04: implementation + hermetic-test fix cycle complete. Five leaking test files fixed via a suite-wide hermetic-home guard (`tests/setup.ts` swaps HOME/USERPROFILE per file) plus explicit deviceId/registryPath seam injections; the 2 CLI tests now pass on the natural run on a used machine, plus a used-machine canary (populated `~/.apiary` md5-unchanged). Orchestrator independently re-verified: typecheck OK, 60 files / 654 tests green natural run, home canary clean before and after. VERIFIED pending W3 close-out.) |
| doctor 004a | a-AC-1..7 | W1-D | DONE (apiary-root.test.ts, apiary-workspace-migration.test.ts, config.test.ts; AC-tagged) |
| doctor 004b | b-AC-1..8 | W1-D | DONE (apiary-registry.test.ts, device-id.test.ts, capture.test.ts; merge rule + migration marker + no-write-back proven) |
| doctor 004c | c-AC-1..8 | W1-D | DONE (apiary-supervision.test.ts; trust-root accept/reject incl. traversal + cross-product rejection) |
| honeycomb 072 index | AC-1..AC-10 | W1-H | VERIFIED (2026-07-04: QA re-audit PASS WITH WARNINGS after the Critical-fix cycle; both Criticals re-attacked and confirmed closed; `npm run ci` green at 4127 tests across worker, orchestrator, and re-auditor runs. One deferred Warning tracked for W4 window close: true packaged-upgrade smoke once a pre-072 installable build exists.) |
| honeycomb 072a | AC-072a.1.1-.4, .2.1-.4, .3.1-.3 | W1-H | DONE (fleet-root.test.ts, lock-continuity.test.ts, migrate.test.ts) |
| honeycomb 072b | AC-072b.1.1-.3, .2.1, .3.1-.2, .4.1 | W1-H | DONE (families.test.ts, memory-mount-migration.test.ts, nectar-recall-precedence.test.ts) |
| honeycomb 072c | AC-072c.1.1-.2, .2.1-.2, .3.1-.2 | W1-H | DONE (fleet-registry-window.test.ts, device-migration.test.ts; absolute paths per ADR Resolved decision 4) |
| honeycomb 072d | AC-072d.1.1-.2, .2.1-.2, .3.1 | W1-H | DONE (daemon-service-pinning.test.ts incl. System32-home pin-wins case) |

**Repo-local flag (honeycomb, non-blocking):** the committed tree is not biome-normalized (LF/CRLF), so a bare `npm run format` rewrites ~900 files; the W1-H worker scoped its diff back to its own files. Settle `.gitattributes`/biome line-ending config before the next repo-wide format run.
| nectar 020 index | AC-1..AC-8 | W1-N | DONE for the PRD-020 surface (2026-07-04: orchestrator independently re-ran build + typecheck exit 0; `npm test` 697 pass / 3 skip / 2 fail, where BOTH failures are the pre-existing LIVE Deeplake probes, not PRD-020 code; home canary clean, no leak. See the live-test investigation note below.) |
| nectar 020a | a-AC-1..6 | W1-N | DONE (src/apiary-root.ts + test/apiary-root.test.ts; adopted across config/config-file/daemon/cli/telemetry) |
| nectar 020b | b-AC-1..7 | W1-N | DONE (src/state-migration.ts + test/state-migration.test.ts) |
| nectar 020c | c-AC-1..6 | W1-N | DONE (service templates/argv/platform + doctor-registry window write; tests updated) |

**CLOSED investigation (2026-07-04 15:17, nectar live tests):** re-run on the warm `apiary_ci` still exceeds the bounded 200s poll, but the code path is PROVEN: the exact semantic-arm SQL run manually returns both probe rows with correct scores/order (near=1, far=0), and every run's rows eventually appear in the workspace. Root cause is backend ingest-visibility lag on this workspace (one 13:52 probe project still showed 1 of 2 rows 85 minutes later), an environment characteristic to raise with Activeloop, not a nectar regression; the identical suite is green in CI against the warm `nectar_ci`. The tests' bounded poll is deliberate (PRD-018) and stays. Final live proof rides the PR CI (nectar_ci secrets). OSPRY stray-row check: OSPRY org returns `402 insufficient balance` (zero balance), so the pre-swap writes almost certainly never landed and no cleanup is possible or needed until it is topped up. ORIGINAL NOTE FOLLOWS for the record:
**(superseded) OPEN investigation (nectar live tests, blocks nothing but must close before W4 ship):** the two live Deeplake tests (`hive-graph-deeplake.test.ts` round-trip, `hive-graph-search-live.test.ts` vector ordering) fail against the freshly-provisioned `apiary_ci` workspace by polling to exhaustion (~200s each): writes land (orchestrator probes confirm `hive_graph` count 2 / versions 3, filtered `WHERE nectar = ...` reads work, embeddings present), but the tests' combined visibility condition never satisfies. Working hypothesis: fresh-workspace index warm-up / insert-then-query visibility lag documented in the tests themselves, not a code regression (the same tests were green in CI under `nectar_ci`, and every unit test over the same store paths passes). Also note: the pre-13:42 W1-N run may have left equivalent throwaway probe rows in OSPRY/`default` (projects named `nectar-prd005-adapter-selftest-*` / `nectar-prd018h-vector-probe-*`, all marked "safe to delete"); W3 close-out checks and cleans those. Re-run the live pair once during W3 on the now-warm `apiary_ci`; if still red, investigate the query shapes (`getIdentity` / vector ordering) before ship.
| hive 010 index | module checklist items | W1-V | DONE (2026-07-04: implementation + hermetic-test fix cycle complete. Three leak sites fixed (startHive missing migrate/register seams; service install() real-fs migration under fake homes; lock.ts legacy read/cleanup against real homedir), plus a structural `tests/setup/isolate-home.ts` net mirroring honeycomb's. Orchestrator independently re-verified: typecheck OK, 56 files / 373 tests green, home canary clean before and after. VERIFIED pending W3 close-out.) |
| hive 010a | rr-AC-1..11 | W1-V | DONE (tests/shared/apiary-root.test.ts, registry-paths.test.ts, service tests; rr-AC-7 grep evidence: `.honeycomb` only in legacy-paths.ts) |
| hive 010b | mg-AC-1..10 | W1-V | DONE (tests/shared/state-migration.test.ts, lock.test.ts, telemetry/emit.test.ts, installer/token-path.test.ts) |
| hive 010c | rc-AC-1..9 | W1-V | DONE (tests/shared/registry-paths.test.ts; boot ordering migrate -> lock -> register in src/daemon/server.ts) |
| nectar 019 index | AC-1..AC-7 | W2-N (+W2-V) | DONE (2026-07-04: orchestrator independently re-verified build + typecheck exit 0, non-live suite 705 tests / 702 pass / 0 fail / 3 privilege-skips, home canary clean. Endpoint contract diffed against hive's wire zod: compatible, incl. `counts: null` absorbed by the object-level catch. VERIFIED pending W3.) |
| nectar 019a | a-AC-1..7 | W2-N | DONE (active-projects/project-supervisor/project-context/daemon-active-projects test suites; dormant-by-default, multi-root, pathological-root guard) |
| nectar 019b | b-AC-1..7 | W2-N | DONE (brooding-state/projects-api/projects-cli suites; `~/.apiary/nectar/projects.json` store + GET/POST endpoints + CLI verbs) |
| nectar 019d | d-AC-1..7 | W2-N | DONE (gitignore.test.ts; parser fallback behind eligible===null; CLI sites wired; disk-fs default flipped) |

**Scoped limitation (flagged, user decision):** W2-N wired per-project LIVE ENRICHMENT only for the primary active project at boot (brood + watch are fully per-project). No 019 AC requires per-project enrichers; candidate fast-follow if multi-project describe throughput matters. Also: W2-N could not run the Aikido scan (local engine error); W3 security close-out covers the surface instead.
| nectar 019c | c-AC-1..7 | W2-V | DONE (2026-07-04: wire methods + NectarProjectsPanel on the Hive Graph page, AC-named tests; orchestrator independently re-verified typecheck OK + 56 files / 383 tests green + home canary clean. UI codes against the 019b contract; end-to-end contract match re-checked when W2-N lands. VERIFIED pending W3.) |

## Related

- Superproject [`ADR-0002`](../knowledge/private/architecture/ADR-0002-one-line-installer-product-loading-and-install-time-telemetry.md) - the installer surface W3 modifies.
- nectar PRD-019 (`nectar/library/requirements/backlog/prd-019-project-scoped-brooding-activation/`) - consumer of `~/.apiary/nectar/projects.json`; can ship before or after PRD-020 (its state-file path already cites the ADR layout).
- doctor `ADR-0002-service-registration-static-registry-plus-runtime-sqlite` - the registry contract whose location this program moves.

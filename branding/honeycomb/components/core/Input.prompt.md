`Input` — text field. Use `mono` for memory keys, recall queries, ids, and paths (the texture of trust). Focus lights the honey ring.

```jsx
<Input placeholder="how do we deploy…" mono iconLeft={searchIcon} />
<Input defaultValue="default" size="sm" />
```

Props: `mono`, `size` (sm/md/lg), `iconLeft`, `disabled`, plus standard value/onChange.
